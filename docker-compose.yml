services:
#   db:
#     image: postgis/postgis:16-3.4-alpine
#     platform: linux/amd64
#     container_name: personal-website-db
#     environment:
#       POSTGRES_DB: personal_website
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     ports:
#       - "5432:5432"
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres -d personal_website"]
#       interval: 5s
#       timeout: 5s
#       retries: 5
# 
  backend:
    build: ./backend
    platform: linux/amd64
    container_name: personal-website-backend
    environment:
      - DJANGO_SECRET_KEY=django-insecure-dev-key-change-in-production
      - DJANGO_DEBUG=True
      - DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend
      - POSTGRES_DB=personal_website
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
      - MAPBOX_ACCESS_TOKEN=your_mapbox_token_here
    volumes:
      - ./backend:/app
    ports:
      - "8001:8001"
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             gunicorn --bind 0.0.0.0:8001 --reload config.wsgi:application"

  frontend:
    build: ./frontend
    platform: linux/amd64
    container_name: personal-website-frontend
    environment:
      - DJANGO_API_URL=http://backend:8001
      - MAPBOX_ACCESS_TOKEN=your_mapbox_token_here
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
    depends_on:
      - backend
    command: npm run dev

# volumes:
#   postgres_data:
